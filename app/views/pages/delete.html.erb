
    <%= link_to("Back to List", {:action => 'index'}, :class => 'back-link') %>
    <div class="subjects edit">
        
        <%= form_for(:page, :url => {:action => 'destroy', :id => @page.id }) do |p| %>
        <br> <%=  @page.name %> </br>
        <%= submit_tag("Delete Page") %>

        <% end %>
    </div>
