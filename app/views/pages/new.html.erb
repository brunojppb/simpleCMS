<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Page Show</title>
</head>
<body>
    <%= link_to("Back to List", {:action => 'index'}, :class => 'back-link') %>
    <div class="page new">
        
        <%= form_for(:page, :url => {:action => 'create'}) do |p| %>

        <table summary="Page form fields">
            <tr>
                <th>Name</th>
                <td><%= p.text_field(:name) %></td>
            </tr>
            <tr>
                <th>Permalink</th>
                <td><%= p.text_field(:permalink) %></td>
            </tr>
            <tr>
                <th>Position</th>
                <td><%= p.text_field(:position) %></td>
            </tr>
            <tr>
                <th>Visible?</th>
                <td><%= p.text_field(:visible) %></td>
            </tr>
        </table>

        <div class="form-buttons">
            <%= submit_tag("Create Page") %>
        </div>

        <% end %>
    </div>
</body>
</html>